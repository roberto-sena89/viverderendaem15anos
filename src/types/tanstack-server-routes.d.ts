// Garante que a augmentação de tipos do TanStack Start (opção `server` nas rotas
// de arquivo, usada pelos endpoints HTTP em src/routes/api e /mcp) esteja sempre
// carregada no programa TypeScript, independente da ordem de resolução dos módulos.
/// <reference types="@tanstack/start-client-core" />
/// <reference types="@tanstack/react-start" />

export {};
